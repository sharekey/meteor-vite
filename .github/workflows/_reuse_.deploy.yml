name: 'Deploy Preview'
run-name: '🚀 Deploy: ${{ inputs.displayName }}'
on:
  workflow_call:
    inputs:
      displayName:
        type: string
        description: Name of the app being deployed
        required: true
      appDir:
        type: string
        description: Basename for the directory containing the example app being deployed.
        required: true
      imageRegistry:
        type: string
        default: ghcr.io
jobs:
  build:
    runs-on: ubuntu-latest
    name: Build
    outputs:
      imageTag: ${{ steps.image.outputs.shaTag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Format image name
        id: image
        # language="bashpro shell script"
        run: |
          imageName="${{ inputs.imageRegistry }}/${{ github.repository_owner }}/meteor-vite.example.${{ inputs.appDir }}"
          imageName="$(echo $imageName | tr '[:upper:]' '[:lower:]')"
          echo "imageName=$imageName" >> $GITHUB_OUTPUT
          echo "cacheTag=$imageName:buildcache" >> $GITHUB_OUTPUT
          echo "shaTag=$imageName:${GITHUB_SHA:0:7}"

      - name: Prepare metadata
        uses: docker/metadata-action@v5
        id: meta
        with:
          images: |
            ${{ steps.image.outputs.imageName }}

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ steps.image.outputs.shaTag }}
            ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ steps.image.outputs.cacheTag }}
          cache-to: type=registry,ref=${{ steps.image.outputs.cacheTag }},mode=max
          build-args: |
            APP_DIR=./examples/${{ inputs.appDir }}

  deploy:
    name: 'Deploy'
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup kubectl
        # language="bashpro shell script"
        run: |
          mkdir -p ~/.kube
          echo '${{ secrets.KUBE_CONFIG_DATA }}' | base64 -d > ~/.kube/config

      - name: Apply manifest
        env:
          APP_NAME: ${{ inputs.appDir }}
          APP_VERSION: ${{ github.ref }}-${{ github.sha }}
          APP_IMAGE: ${{ needs.build.outputs.imageTag }}
        # language="bashpro shell script"
        run: |
          cat .kubernetes/deployments/meteor.yml | envsubst | kubectl apply -f -
